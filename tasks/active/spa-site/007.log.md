---
kind: log
date: 2026-06-13
---

# Status: paused -> active (resume)

Resuming to build the site now that the engine is published. Key shift since this
task was paused: the engine was rewritten source-agnostic and published as
`jsr:@kindkitchen/genv` — the playground reuses it directly (in-browser via
esm.sh, no build step). The old action-coupled merge analysis [[002.analysis]] is
superseded by the engine's public API (genv/merge_sources over ordered
SourceInput; parse/stringify).

Open decision carried into the session: whether the playground exposes the
generic engine (ordered sources) or the GitHub action inputs (via a small in-site
adapter mirroring parity's from_action).
