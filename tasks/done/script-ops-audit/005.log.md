---
kind: log
date: 2026-06-12
---

# Retargeted cumulative suite to v1.0.1

User bumped VERSION.MD/README to v1.0.1 ("improve testing") in commit 9c2311a;
action.yml unchanged. Per user choice, renamed the suite
`e2e.self-invocation.v1.0.0.yml` -> `...v1.0.1.yml` and bumped `name`, the
trigger tag, and all six `uses: kindkitchen/genv@v1.0.0` -> `@v1.0.1`. Kept the
`# since v1.0.0` historical annotation (that scenario was introduced in v1.0.0).

Known consequence the user accepted: `@v1.0.1` is not published yet, so the PR
CI for this suite will be RED until the v1.0.1 tag is pushed; it goes green only
after release. This is the self-invocation timing limitation flagged in
[[001.decision]] item 1.

Still blocked: awaiting v1.0.1 tag/publish + green CI before moving to done.
