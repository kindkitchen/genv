---
kind: log
date: 2026-06-13
---

# Published jsr:@kindkitchen/genv@0.1.0

Engine published to JSR (latest 0.1.0). Added MIT `LICENSE` and a
`publish.exclude` in deno.json (drops `*.test.ts`, `assert.ts`, and the agent md
files from the package). Submodule advanced to 8350ca8 ("prepare to publish");
parent pointer bumped to match. The engine commits remain unpushed to the genv-js
git remote (user's call) — only the JSR artifact is public.

Consumers can now `import { genv, parse, merge_sources, ... } from
"jsr:@kindkitchen/genv"`; the package re-exports genv/merge_sources/SourceInput,
parse/stringify, filter/FilterSpec, genv_cli.
