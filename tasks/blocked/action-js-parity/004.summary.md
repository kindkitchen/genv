---
kind: summary
date: 2026-06-13
---

# Adapter + parity harness implemented and green; status todo -> active

Built per [[003.decision]]. Status moves todo -> active (adapter + local parity
done; CI wiring still pending).

Files in `parity/`:
- `adapter.ts` — `from_action(inputs): SourceInput[]` mapping the 5 action steps
  to ordered sources (content_from_vars = `include`+`content` on the same
  pattern; vars kv = include/exclude; same for secrets; literal last). vars_obj/
  secrets_obj JSON strings are `JSON.parse`d to objects (engine `data` accepts an
  object). Plus `genv_action(inputs)` = `genv(from_action(...))`. `ActionInputs`
  mirrors action.yml with its defaults (impossible exclude sentinel, etc.).
- `action-runner.ts` — `run_action(overrides)` runs the real action.yml steps.
- `fixtures.ts` — 8-case corpus (defaults, kv, include, exclude, content_from,
  secrets-outrank-vars, dotenv_content priority, full pipeline).
- `parity.test.ts` — per fixture: `parse(run_action)` vs `parse(genv(from_action))`,
  deep-equal. `deno.json` test task: `--allow-run=bash --allow-read --allow-write
  --allow-env`, `@std/yaml` import.

Green: 8/8 `deno task test`, lint + fmt clean. Not committed yet.

Next: CI wiring (thin/fast, per project ethos; e2e.* cover each version) so parity
runs in CI; optionally widen the corpus (number/bool @json values, regex-dialect
edge cases). Then JSR publish of the engine unblocks importing it by `jsr:` here
instead of the submodule path.
