---
kind: log
date: 2026-06-13
---

# CI: parity workflow added

Added `.github/workflows/parity.yml`: on push to main/dev and PRs (path-filtered
to `action.yml` / `parity/**` / the workflow file), checkout + setup-deno +
`deno task test` in `parity/`. Thin/fast per project ethos; kept separate from the
bash `e2e.self-invocation.*` workflows (those assert the published action by tag;
this compares action.yml against the engine).

No submodule checkout needed (parity imports `jsr:@kindkitchen/genv`); jq/bash are
preinstalled on ubuntu. Locally green (8/8); not yet exercised on GitHub since the
parent isn't pushed.
